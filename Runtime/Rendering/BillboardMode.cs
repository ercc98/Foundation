namespace ErccDev.Foundation.Rendering
{
    public enum BillboardMode
    {
        Spherical,   // Fully faces the camera on all axes
        Cylindrical  // Rotates only around the world Y axis (stays upright)
    }
}
